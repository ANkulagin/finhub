version: 2

run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - unused
    - revive

linters-settings:
  formatter:
    formatters:
      - gofmt
      - goimports
